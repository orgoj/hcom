# TODO

- [ ] Consider supporting `SOUL.md`, or a catalog-configurable instruction filename, in `.hcom/agents/<name>/` bundle directories instead of `AGENTS.md`. This would avoid collisions with repository-level `AGENTS.md` files and prevent harnesses such as Claude, Codex, and AGY from automatically loading or indexing bundle instructions as workspace rules.
